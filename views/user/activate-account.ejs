<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Activate Account</title>
    <link rel="stylesheet" href="/stylesheets/user/activate-account.css">
</head>
<body>
    <div id="div1">
        <div class="flashbox">
            <% if(success && success.length) { %>
                   <div class="flashboxsuccess-alert">
                       <%= success %> 
                       <span class="flashboxsuccess-closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
                       
                   </div>
               <% } %> 
           
               <% if(error && error.length) { %>
                       <div class="flashboxerror-alert">
                           <%= error %> 
                           <span class="flashboxerror-closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
                           
                       </div>
               <% } %> 
               
        </div>
        <div id="div1box">
            
            <h3>Congratulations <%= user.username %>!</h3>
            <p>You have successfully created an account and an activation link has been sent to your email: <b><u><%= user.email %></u></b></p>
            <p>Please click on the activation link to activate your account.</p>
            <p>Thank you!</p>

            <form action="/user/<%= user.id %>/activate-account" method="post">
                <button class="resendbtn">Resend Activation Link</button>
            </form>
        </div>
    </div>
    
</body>
</html>